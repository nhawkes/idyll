[package]
name = "idyll-schema"
version = "0.1.0"
edition = "2021"
description = "The published idyll schema artifact: types + deterministic serialization, shared by the data layer and the client macros."
license = "MIT"
repository = "https://github.com/nhawkes/idyll"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
