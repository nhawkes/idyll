[package]
name = "idyll-wit"
version = "0.1.0"
edition = "2021"
description = "Vibe coded web framework"
license = "MIT"
repository = "https://github.com/nhawkes/idyll"

[features]
host = ["dep:wasmtime"]

[dependencies]
wasmtime = { version = "46", features = ["component-model", "runtime"], optional = true }
